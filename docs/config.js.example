// Optional: copy this file to config.js and fill in your key(s) for local use.
// config.js is listed in .gitignore so your keys are never committed.
//
// In production the site loads without this file — users enter their own
// API keys directly on the page and they are stored in localStorage only.
window.AIRING_GEMINI_KEY = '';
window.AIRING_GROK_KEY   = '';
window.AIRING_CLAUDE_KEY = '';
